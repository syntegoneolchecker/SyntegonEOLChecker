{
  "name": "syntegon-eol-checker",
  "version": "1.0.0",
  "description": "CSV Database Manager for EOL tracking",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^2.8.2",
    "@tavily/core": "^0.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
